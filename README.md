# scala-rc-circe-json-validator

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/scala-rc-circe-json-validator` and run via `bowtie run -i scala-rc-circe-json-validator`.
